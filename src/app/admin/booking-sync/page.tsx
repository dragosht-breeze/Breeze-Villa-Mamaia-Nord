import BookingSyncCenter from "@/components/admin/booking-sync/BookingSyncCenter";

export const metadata = {
  title: "Booking Sync | Breeze PMS",
};

export default function BookingSyncAdminPage() {
  return <BookingSyncCenter />;
}
